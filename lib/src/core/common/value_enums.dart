enum UserRole { investor, admin, team }

enum UserStatus { active, pending, blocked }

enum KycStatus { pending, approved, rejected }

enum TransactionType { deposit, withdrawal, profit, adjustment }

enum TransactionStatus { pending, approved, completed }
