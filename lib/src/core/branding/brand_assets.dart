/// Single brand raster: replace [logoPng] on disk to update launcher icons
/// (`dart run flutter_launcher_icons`) and every in-app `Image.asset` use.
abstract final class BrandAssets {
  static const String logoPng = "assets/branding/app_brand.png";
}
